
provider "vsphere" {
  user           = "username@vsphere.local"
  password       = "your_password"
  vsphere_server = "vcenter_fqdn_or_ip"

  # If you have a self-signed cert
  allow_unverified_ssl = true
}